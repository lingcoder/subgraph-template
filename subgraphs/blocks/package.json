{
  "name": "blocks",
  "license": "MIT",
  "description": "blocks subgraph",
  "author": "LingCoder",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node undefined lingcoder/blocks",
    "create-local": "graph create --node http://localhost:8020/ lingcoder/blocks",
    "remove-local": "graph remove --node http://localhost:8020/ lingcoder/blocks",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lingcoder/blocks",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.41.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
